/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'image.api.playstation.com',
      'example.com', 
      'rexus.id',
    ],
  },
}

export default nextConfig;
